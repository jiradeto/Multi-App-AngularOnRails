{
  "private": true,
  "scripts": {
    "postinstall": "cd admin-app && npm install && cd ../web-app && npm install",
    "build:prod": "rm -rf public/wassets/* && webpack --progress --config webpack.prod.js --bail",
    "build": "rm -rf public/wassets/* && webpack --progress --color",
    "build:watch": "rm -rf public/wassets/* && webpack --progress --color --watch"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}